# /home is a symlink to /var/home and /var is not part of a bootc image, so the
# core user's home directory cannot be created by useradd at build time.
d /var/home/core 0700 core core -
d /var/lib/pi-core 0755 root root -
